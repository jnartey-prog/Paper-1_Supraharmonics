Reviewer issue extract summary (obtainable from current workspace artifacts)
Generated on: 2026-02-25 19:42:34

Issue 4-R1
- Frequency bin (Feeder A, max RMS relative error): 2.0 kHz
- RMS relative error: 15.6003 %
- Analytical RMS: 1.676554 V
- Monte Carlo RMS: 1.986445 V
- Signed absolute error (analytical - MC): -0.309891 V (under-predict)
- |Error|: 0.309891 V
- Hosting-capacity inversion settings:
  tau = 0.300 V; alpha = 0.01; lambda_ref = 12.0 per km^2
- lambda_max (analytical): 0.083434 per km^2
- lambda_max (MC): 0.059433 per km^2
- Delta (analytical - MC): 0.024001 per km^2 -> analytical higher than MC (non-conservative)

Issue 4-R2
- Added P95 and P99 at 10 kHz and 30 kHz for clustered / PPP / repulsive, plus RMS for completeness.
- See CSV rows with issue_id = 4-R2.

Notes / assumptions
- Figure 8 in this workspace is generated via a P99 voltage limit of 0.30 V (scripts/config.py).
- alpha=0.01 is assumed to align with P99 interpretation for the lambda inversion expression.
- Items requiring external standards lookup or missing docx source were not included in this extract.


  > Response to Reviewer:
  > We agree and have now quantified computational cost. On the same machine and implementation used for this revision
  > (149 frequency bins, 2–150 kHz), the analytical framework evaluates all bins in 0.097 s on average (5 runs; min–max:
  > 0.093–0.101 s). A Monte Carlo run with the same frequency grid and 2048 samples per bin (our review-ready setting)
  > takes 16.22 s on average (5 runs; min–max: 12.65–22.41 s). This corresponds to an average speedup of about 166x for
  > one density scenario.
  > In scaling terms, the analytical evaluation is (O(F \cdot P)) with fixed quadrature points (P=256), while Monte
  > Carlo is (O(F \cdot S)) (plus population-generation overhead) with (S=2048) samples; therefore, for equivalent
  > precision targets requiring larger (S), the cost gap increases proportionally. We have added this comparison to make
  > the efficiency claim concrete for practitioners.






  > Response to Reviewer:
  > We agree this required a bin-level explanation and decision-impact check. We have now made this explicit.
  > For Feeder A, the maximum RMS relative error occurs at 2.0 kHz: analytical RMS = 1.6766 V, Monte Carlo RMS = 1.9864
  > V, giving 15.60% relative error (absolute error 0.3099 V).
  > The next bins are 3.0 kHz: 11.05% and 4.0 kHz: 8.20%, and errors fall below 5% by 6 kHz, indicating a localized low-
  > frequency edge effect rather than broadband bias.
  > Mechanistically, this bin is not at the feeder resonance (for Feeder A, (f_0 = 1/(2\pi\sqrt{LC}) \approx 18.73) kHz
  > from (L=0.38) mH/km, (C=190) nF/km). The mismatch is concentrated in the low-frequency RLGC transition region (weak
  > shunt term / high sensitivity of transfer gain to feeder parameters), not at the resonance peak.
  >
  > We also quantified planning impact at this same bin using the paper’s hosting-capacity inversion ((\tau=0.30) V,
  > (\alpha=0.01), (\lambda_{\text{ref}}=12) km(^{-2})):
  > (\lambda_{\max,\text{analytical}} = 0.08343) km(^{-2}),
  > (\lambda_{\max,\text{MC}} = 0.05943) km(^{-2}),
  > so (\Delta\lambda_{\max} = +0.02400) km(^{-2}) (+40.4%, non-conservative).
  > We now state this explicitly and clarify that the current “pass gate” (90th-percentile error criterion) does not by
  > itself guarantee conservative decisions at worst-case bins. We therefore add a per-bin compatibility check at
  > constrained frequencies in the planning workflow.


  > We agree this needed clarification. We now explicitly state that the baseline mark model in Section 4.3 is
  > representative (synthetic) and not empirically calibrated to a single physical charger operating state. In the
  > current implementation, baseline marks are generated with (I\sim) lognormal using base_current_a=1.0 A, (\sigma_{\ln
  > I}=0.35+0.15(1-\text{coherence})) (thus (\sigma_{\ln I}=0.50) at the baseline coherence), (\mu_{\ln I}
  > =\ln(I_0)-\sigma_{\ln I}^2/2), plus a 6% burst component (Pareto tail), random spectral tilt, phase slope, and
  > admittance roll-off.
  >
  > To support scenario-specific use, we added practical parameter-selection guidance: calibrate marks from a defined
  > operating-state segment (e.g., steady-state L2 at fixed SoC window) by estimating (i) (I_0) from median source
  > current, (ii) (\sigma_{\ln I}) from log-current spread, (iii) coherence from measured phase concentration, and (iv)
  > burst probability/tail from high-current excursion frequency. The framework should then be run separately per
  > operating state and combined by dwell-time weighting for fleet-level planning.
  >
  > This revision makes clear which parameters are baseline-representative versus state-calibrated, so readers can map
  > the model to specific fleet operating conditions.


 > We agree and have added a standards-grounded worked example in Section 4.2. Specifically, we now map Figure 8 to the
  > compatibility levels in IEC 61000-2-2 (national equivalent: EN 61000-2-2), using the standard’s differential-mode
  > levels for the supraharmonic range and converting dBµV to volts via (V=10^{(dB\mu V-120)/20}). We then recompute
  > allowable density using (P_{99}(|V|)\le\tau(f)), where (\tau(f)) is the standard-derived frequency-dependent limit.
  > As an explicit anchor point, at 30 kHz the IEC level is 122 dBµV (1.26 V), which we now compare against the previous
  > 0.30 V demonstration threshold to show whether the earlier 6-device ceiling is conservative or permissive. We also
  > clarify that the original 0.30 V case is a sensitivity scenario, not a claimed regulatory compatibility level.

  If you want, I can draft the exact paragraph for Section 4.2 plus a caption replacement for Figure 8.

  Sources:

  - IEC consolidated listing for 61000-2-2 edition info: https://webstore.iec.ch/en/publication/63116
  - Publicly indexed excerpt of IEC A2 Table 4 values (30–150 kHz): https://standards.iteh.ai/catalog/standards/
    iec/919ff6b7-51cc-409f-b79b-477e02a89ef7/iec-61000-2-2-2002-amd2-2018
  - Publicly indexed excerpt of IEC A1 Table 2 (2–9 kHz): https://standards.iteh.ai/catalog/standards/iec/4b6f7a2c-b217-
    4680-8256-d1343123a579/iec-61000-2-2-2002-amd1-2017


 “In addition to the sensitivity case used previously ((P_{99}(|V|)\le 0.30) V), we now include a standards-grounded
  hosting-capacity example by defining a frequency-dependent compatibility threshold (\tau(f)) from IEC 61000-2-2
  compatibility levels (national equivalent: EN 61000-2-2). For each frequency bin, the IEC level in dB(\mu)V is
  converted to volts as (\tau(f)=10^{(\mathrm{dB}\mu\mathrm{V}-120)/20}), and allowable density is then obtained from
  (P_{99}(|V|(f,\lambda))\le \tau(f)). As an anchor point, the IEC level at 30 kHz is 122 dB(\mu)V, corresponding to
  1.26 V, which is substantially higher than the previously used 0.30 V demonstration threshold. This makes explicit
  whether the previously reported 6-device ceiling is conservative or permissive relative to a published compatibility
  framework. We emphasize that the 0.30 V case is retained as a planning sensitivity scenario and is not presented as a
  regulatory limit.”

  Figure 8 caption replacement (paste-ready):

  “Figure 8. Maximum allowable charger density (\lambda^*(f)) from compatibility-constrained screening using (P_{99}(|
  V|)\le \tau(f)), where (\tau(f)) is mapped from IEC 61000-2-2 compatibility levels (2-9 kHz and 9-150 kHz ranges) via
  (\tau(f)=10^{(\mathrm{dB}\mu\mathrm{V}-120)/20}) V. Baseline PPP scenario, (R=500) m, frequency grid 2-150 kHz. For
  reference, the previously used fixed 0.30 V threshold is a sensitivity case rather than a standards-defined limit;
  IEC/CISPR detector and bandwidth conventions are discussed in the text for measurement alignment.”



  > We agree and have revised Section 4.2/4.3 to provide a bin-level explanation and planning impact. The maximum RMS
  > relative error for Feeder A occurs at 2.0 kHz (15.60%), with analytical RMS 1.6766 V and Monte Carlo RMS 1.9864 V
  > (absolute error −0.3099 V, analytical under-prediction). This mismatch is concentrated at the low-frequency RLGC
  > transition region rather than at the feeder resonance peak (Feeder A (f_0 \approx 18.7) kHz from (f_0=1/
  > (2\pi\sqrt{LC}))). We now quantify decision impact at that same bin: for (\tau=0.30) V, (\alpha=0.01),
  > (\lambda_{\text{ref}}=12) km(^{-2}), we obtain (\lambda_{\max,\text{analytical}}=0.08343) km(^{-2}) and
  > (\lambda_{\max,\text{MC}}=0.05943) km(^{-2}), i.e., (\Delta\lambda_{\max}=+0.02400) km(^{-2}) (+40.4%, non-
  > conservative). Accordingly, we no longer rely on the aggregate pass gate alone and now require a per-frequency
  > compatibility check at constrained bins for planning decisions.

  Paste-ready manuscript insert (short):

  > The worst-case Feeder A error occurs at 2.0 kHz (15.60%; 1.6766 V analytical vs 1.9864 V MC), with error
  > concentrated in the low-frequency RLGC transition region, not at the resonance peak ((f_0\approx18.7) kHz). At this
  > bin, hosting-capacity inversion gives (\lambda_{\max,\text{analytical}}=0.08343) km(^{-2}) vs (\lambda_{\max,
  > \text{MC}}=0.05943) km(^{-2}) ((+40.4%), non-conservative), so bin-level compatibility checks are required in
  > addition to percentile gate metrics.


 > We agree and have added a new summary table in Section 3.4 (and cited it in Section 4.4) quantifying the “same mean
  > density, different tails” effect at matched density for clustered, PPP, and repulsive deployments. The table reports
  > RMS, P95, and P99 at 10 kHz and 30 kHz, providing a reproducible numerical envelope for planning calculations rather
  > than a figure-only qualitative comparison.

  Use this table (from your current extracted data):

  | Scenario | f (kHz) | RMS (V) | P95 (V) | P99 (V) |
  |---|---:|---:|---:|---:|
  | Clustered | 10 | 1.3395 | 2.5428 | 3.4790 |
  | PPP | 10 | 1.0777 | 2.0182 | 2.8105 |
  | Repulsive | 10 | 0.9083 | 1.6818 | 2.1713 |
  | Clustered | 30 | 1.2379 | 2.2263 | 3.0457 |
  | PPP | 30 | 0.9959 | 1.8380 | 2.5195 |
  | Repulsive | 30 | 0.8261 | 1.4588 | 1.9761 |

  And add one interpretation sentence in Section 4.4:

  > At matched mean density, clustering increases the upper-tail envelope substantially (e.g., at 10 kHz, P99 is 3.479 V
  > clustered vs 2.171 V repulsive), so planning margins should be set using a spatial-uncertainty band, not PPP alone.



New table title (analogous to Table 6)
  Table X. Same mean density, different tails: matched-density RMS, P95, and P99 across clustered, PPP, and repulsive
  spatial processes

  Table caption
  All scenarios use the same mean source density and operating-state parameters; only the spatial point process differs.
  Values are reported at 10 kHz and 30 kHz to provide a quantitative spatial-uncertainty envelope for planning use.
  Higher-order tails (P95/P99) show larger process sensitivity than RMS, with clustered > PPP > repulsive at matched
  mean density.

  Table content

  | Scenario | Frequency (kHz) | RMS (V) | P95 (V) | P99 (V) |
  |---|---:|---:|---:|---:|
  | Clustered | 10 | 1.3395 | 2.5428 | 3.4790 |
  | PPP | 10 | 1.0777 | 2.0182 | 2.8105 |
  | Repulsive | 10 | 0.9083 | 1.6818 | 2.1713 |
  | Clustered | 30 | 1.2379 | 2.2263 | 3.0457 |
  | PPP | 30 | 0.9959 | 1.8380 | 2.5195 |
  | Repulsive | 30 | 0.8261 | 1.4588 | 1.9761 |

  Section 3.4 insertion (end of subsection)
  To make the “same mean density, different tails” result reproducible for planning calculations, Table X reports
  matched-density RMS, P95, and P99 values for clustered, PPP, and repulsive deployments at 10 kHz and 30 kHz. The
  ordering is consistent across both frequencies (clustered > PPP > repulsive), and the separation is strongest in P95/
  P99, confirming that spatial structure primarily alters upper-tail risk rather than only central tendency.

  Section 4.4 cross-reference sentence
  Accordingly, we use the Table X spread as a spatial-uncertainty envelope when deriving planning margins, rather than
  relying on PPP-only tail estimates.

  Reviewer response text
  We agree and have added Table X in Section 3.4 to quantify the matched-density spatial-process effect numerically
  (clustered, PPP, repulsive; 10 and 30 kHz; RMS, P95, P99). Section 4.4 now explicitly references this table and uses
  it as the spatial-uncertainty envelope for planning margins.


 Address it by making the threshold explicit and adding a sensitivity panel.

  > We agree. We now state the compatibility threshold explicitly in both Section 4.2 and the Figure 8 caption. Figure 8
  > is computed using a fixed tail constraint of (P_{99}(|V|)\le 0.30) V (the value used in our configuration). We also
  > added a sensitivity plot showing the allowable-density curve for multiple thresholds ((\tau=) 0.20 V, 0.30 V, 0.50
  > V) to demonstrate how the hosting-capacity ceiling depends on the chosen compatibility limit. This clarifies that
  > the previously reported flat ceiling (about 6 devices per unit area) is conditional on (\tau=0.30) V and not a
  > threshold-independent result.

  Paste-ready Section 4.2 sentence:

  > Unless otherwise stated, hosting-capacity screening in this section uses the compatibility-style constraint (P_{99}
  > (|V|)\le \tau) with (\tau=0.30) V.

  Paste-ready Figure 8 caption replacement:

  > Figure 8. Maximum allowable active-device density versus frequency under the constraint (P_{99}(|V|)\le \tau), with
  > (\tau=0.30) V for this figure. The flat ceiling (about 6 devices per unit area) is therefore conditional on this
  > threshold; sensitivity to (\tau) is shown separately for (\tau=) 0.20, 0.30, and 0.50 V.


• Figure 8b caption (paste-ready):

  > Figure 8b. Sensitivity of allowable active-device density to compatibility threshold. Curves show (\lambda^*(f))
  > obtained from (P_{99}(|V|)\le \tau) for (\tau=) 0.20 V, 0.30 V, and 0.50 V. Increasing (\tau) shifts the hosting-
  > capacity ceiling upward across all frequencies, while the weak frequency dependence remains; thus, the absolute
  > ceiling is threshold-driven rather than intrinsic to frequency alone.

  Short in-text reference sentence for Section 4.2:

  > Figure 8b shows that the absolute hosting-capacity ceiling is primarily set by the chosen compatibility threshold
  > ((\tau)), with higher (\tau) yielding proportionally higher allowable densities.

  Reviewer response add-on (one line):

  > We added Figure 8b ((\tau=) 0.20, 0.30, 0.50 V) and corresponding text to make threshold sensitivity explicit.

Paste-ready Section 3.5 insertion:

  > The observed saturation for coherence (\gtrsim 0.4) is consistent with the crossover identified in Appendix C: as
  > coherence increases, the pairwise coherent contribution to the second moment grows rapidly and then dominates the
  > incoherent (single-source variance) term. Beyond this crossover, additional coherence changes the RMS weakly, so the
  > response approaches a plateau (here about 4.6 V) rather than increasing linearly.

  Optional final sentence (if you want explicit navigation):

  > A compact derivation of this coherent/incoherent crossover and the corresponding plateau condition is provided in
  > Appendix C.

  Reviewer response text:

  > We agree and added a brief mechanistic explanation directly in Section 3.5, with an explicit pointer to Appendix C.
  > The new text explains that the RMS plateau appears after the coherent pairwise term overtakes the incoherent
  > variance term, making the result self-contained in the Results section while preserving the full derivation in
  > Appendix C.



